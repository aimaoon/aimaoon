# 仕様スナップショット

**確認日:** 2026-08-27

## 公式ページの確認状況（重要）

| URL | 状態 |
|---|---|
| https://creator.line.me/ja/guideline/animationemoji/ | **取得不可**（`EGRESS_BLOCKED`: 実行環境のネットワークプロキシが `creator.line.me` を遮断） |
| https://creator.line.me/ja/review_guideline/ | **取得不可**（同上） |
| https://creativelab-tips.line.me/ja/apng_size | **取得不可**（同上） |

本セッションの実行環境からLINE公式ドメインへ到達できないため、**公式ページの
現行数値を直接確認できていない**。以下の数値は本プロジェクトの指示書に記載された
値をそのまま採用したもので、`検証状態` 列のとおり未検証である。

公式仕様と本表が異なる場合、優先すべきは公式仕様。**提出前にユーザー自身で
公式ページを開き、下表を照合すること。**

## 採用した制作仕様

| 項目 | 採用値 | 検証状態 |
|---|---|---|
| 画像数 | 40個 | 未検証（指示書より） |
| 画像サイズ | 180 × 180 px | 未検証（指示書より） |
| ファイル形式 | APNG（PNG colour type 6 = RGBA, 8bit） | 未検証（指示書より） |
| 背景 | 完全透過 | 未検証（指示書より） |
| DPI | 指定なし（PNGに `pHYs` チャンクを書き込まない） | 未検証 |
| フレーム数 | 20 / 個 | 未検証（指示書より） |
| 1周の再生時間 | 4,000 ms | 未検証（指示書より） |
| ループ回数 | 1（APNG `acTL.num_plays = 1`） | 未検証（指示書より） |
| 総再生時間 | 4,000 ms（4秒以内） | 未検証（指示書より） |
| 1ファイル容量 | 300,000 bytes 以下（制作目標 285,000 bytes 以下） | 未検証（指示書より） |
| ZIP内ファイル名 | `001.png` 〜 `040.png`、ZIP直下にフラット配置 | 未検証（指示書より） |
| ZIP全体の容量上限 | **不明**（公式値を確認できず） | 未検証 |
| tab画像 / main画像 | 本ZIPには含めていない | **要確認** |

## 外部で確認が必要な項目

1. 上表すべての数値（特にフレーム数の上下限、ループ回数の許容範囲、ZIP全体の容量上限）
2. アニメーション絵文字の提出物に `tab.png` / `main.png` が必要かどうか。
   本プロジェクトは40枚のAPNGのみを `LINE_READY.zip` に格納している
3. 生成AI利用申告の要否と申告文言（`store-listing-text.md` に案を用意した）
4. 審査ガイドラインの現行の禁止事項一覧

## 生成物が実際に満たしている値（実測）

`reports/manifest.json` の各ファイルはAPNGのチャンク列から直接読み取った実測値。

- 全40ファイル: 180 × 180、colour type 6、bit depth 8
- 全40ファイル: `fcTL` 20個、遅延合計 4,000 ms、`acTL.num_plays` = 1
- 最大ファイル: 220,932 bytes（目標 285,000 / 上限 300,000 を下回る）
- 補助テキストチャンク（`tEXt`/`iTXt`/`tIME`/`pHYs`）: 全ファイル 0 個
