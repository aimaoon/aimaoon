# 通知サーバー（worker/ を Cloudflare にデプロイすると発行される URL）
VITE_PUSH_API=https://stage-note-push.<あなた>.workers.dev

# VAPID の公開鍵。worker 側の VAPID_PUBLIC_KEY と同じ値を入れる。
VITE_VAPID_PUBLIC_KEY=

# worker で APP_TOKEN を設定した場合だけ。
VITE_PUSH_TOKEN=
