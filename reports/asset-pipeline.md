# asset pipeline

asset_pipeline_mode: UNDETERMINED

## 状況（第0-5節）

Claude Code には組み込み画像生成ツールが存在しない。本セッション開始時点で、
リポジトリ内に原画は1枚も存在しない（`poses/` `masks/` は空、`character-master.png` なし）。

したがって第0-5節の定めにより、正しい初動は「第1節のヒアリングとデザイン提案（テキスト）＋調達モードの確認」であり、
コードで円・楕円・多角形を組み合わせてキャラクターを描き起こすことは**禁止**されている。

## 調達モードの選択肢

| モード | 内容 | 本環境での可否 |
|---|---|---|
| A：ユーザー提供原画 | ユーザーが基準キャラクター1枚と各絵文字の専用ポーズ原画（01〜32は各6枚＝192枚、33〜40は各5状態）を提供。Claudeはマスク抽出・部位分離・リグ・中割り・APNG化・検査を担当 | 可能（既定モード） |
| B：画像生成API | ユーザーがエンドポイントとキーを提供し、Claudeが `scripts/gen_pose.py` から呼び出す | egress制限あり。`creator.line.me` は遮断済みで、APIホストへの到達性は未確認。キー提示と到達性確認が別途必要 |
| C：ベクター原画 | ユーザーが承認したSVG原画をパラメトリックに部位変形 | ユーザーがSVGを提供・承認した場合のみ可能 |
| D：別環境で生成 | ユーザーが別環境で原画を作りこのセッションへ配置。Claudeは設計・検査・アセンブリ・提出物作成を担当 | 可能 |

## 決定

未決定。ユーザーの選択待ち。

## 記録

| 日時 (UTC) | 事象 |
|---|---|
| 2026-09-02T03:18:02Z | 環境プリフライト実行。Pillow/numpy 未導入 → pip install で導入成功。apngasm/oxipng/ffmpeg は NOT_FOUND |
| 2026-09-02T03:18:26Z | `creator.line.me` 2URL とも EGRESS_BLOCKED。第2-1節の暫定値運用へ分岐 |
